# Plan 04-07: Helldivers 2 Bundle

**Wave**: 4
**Depends on**: 04-03-PLAN.md
**Autonomous**: true
**Files modified**: `profiles/hd2.yaml` [NEW], `build.rs` or `src/main.rs` (for bundling)

## Goal
Create the comprehensive Helldivers 2 stratagem pack and ensure it's available to the user.

## Tasks

<task name="hd2_pack_data" read_first="ROADMAP.md">
<action>
Create `profiles/hd2.yaml` containing all 80+ stratagems grouped by category:
- Patriotic Administration Center (Support Weapons)
- Orbital Cannons
- Hangar (Eagles)
- Bridge
- Engineering Bay
- Robotics Workshop
Include correct key sequences for all (UP, DOWN, LEFT, RIGHT).
</action>
<acceptance_criteria>
`profiles/hd2.yaml` exists and covers all current stratagems.
YAML is valid against the `Pack` schema.
</acceptance_criteria>
</task>

<task name="default_bundle" read_first="src/main.rs">
<action>
Ensure the `hd2.yaml` (and a default `.hdpack`) is bundled with the application or installed to the config dir on first run.
Add a `hd-voice install-hd2` command or similar.
</action>
<acceptance_criteria>
A new user can easily get the HD2 stratagems ready for use.
</acceptance_criteria>
</task>

## Verification
- Run `hd-voice switch hd2`.
- Verify several stratagems from different categories match and fire.
