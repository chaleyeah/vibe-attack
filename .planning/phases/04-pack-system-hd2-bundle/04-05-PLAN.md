# Plan 04-05: TUI Editor - Framework

**Wave**: 3
**Depends on**: 04-01-PLAN.md, 04-03-PLAN.md
**Autonomous**: true
**Files modified**: `src/tui/mod.rs` [NEW], `src/tui/app.rs` [NEW], `src/main.rs`

## Goal
Set up the `ratatui` TUI framework and implement the category/macro browser.

## Tasks

<task name="tui_setup" read_first="Cargo.toml">
<action>
Create `src/tui/mod.rs` to handle terminal initialization (entering raw mode, alternate screen) and the main event loop (keyboard input).
Use `crossterm` for the backend.
</action>
<acceptance_criteria>
`hd-voice edit` enters a full-screen TUI.
Pressing 'q' exits correctly and restores the terminal.
</acceptance_criteria>
</task>

<task name="category_browser" read_first="src/pack/mod.rs">
<action>
Implement the `App` state in `src/tui/app.rs`.
- Load the current profile's `Pack`.
- Render a sidebar with Categories and a main list with Macros in the selected category.
- Support navigation with Arrow keys/Tab.
- Use a "Tactical" theme (Yellow/Gray, heavy borders).
</action>
<acceptance_criteria>
The TUI displays all categories and macros from the active pack.
Navigation works correctly.
</acceptance_criteria>
</task>

## Verification
- Run `hd-voice edit` and verify the layout and colors.
- Navigate through categories and macros.
