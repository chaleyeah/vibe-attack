---
phase: 02-pipeline-core
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - docs/latency-baseline.md
  - tests/concurrency_stress.rs
  - .planning/phases/02-pipeline-core/02-VALIDATION.md
autonomous: true
requirements: [STT-04]
user_setup: []

must_haves:
  truths:
    - "There is a reproducible procedure to prove the Phase 2 latency budget on target hardware."
    - "There is a stress test artifact proving concurrency (audio capture + recognition) under load."
    - "Nyquist validation bookkeeping reflects Wave 0 readiness once the required test scaffolding exists."
  artifacts:
    - path: "docs/latency-baseline.md"
      provides: "Latency proof procedure + evidence capture + pass/fail criteria"
    - path: "tests/concurrency_stress.rs"
      provides: "Env-gated stress test for concurrent pipeline activity"
    - path: ".planning/phases/02-pipeline-core/02-VALIDATION.md"
      provides: "Updated validation status after Wave 0 is implemented"
---

<objective>
Add the “proof artifacts” that make Phase 2 success criteria verifiable: a repeatable latency baseline procedure (with clear pass/fail criteria) and a concurrency stress test. Also update the phase validation contract to reflect Wave 0 readiness once its files exist.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pipeline-core/02-CONTEXT.md
@.planning/phases/02-pipeline-core/02-RESEARCH.md
@.planning/phases/02-pipeline-core/02-VALIDATION.md
@docs/uinput-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write a reproducible Phase 2 latency baseline procedure (end-of-speech → transcript emit)</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-pipeline-core/02-CONTEXT.md
    - .planning/phases/02-pipeline-core/02-RESEARCH.md
  </read_first>
  <files>docs/latency-baseline.md</files>
  <action>
    Create `docs/latency-baseline.md` documenting a repeatable “Phase 2 latency proof” run on target hardware.

    IMPORTANT: STT-04 in `.planning/REQUIREMENTS.md` mentions “end of speech → first key event”.
    In Phase 2 (no dispatch yet), the measurable proxy is:
    - **end of speech → transcript JSONL emit**
    This doc must state that explicitly and note that “→ first key event” is validated in Phase 3 dispatch while we keep the same <500ms budget proxy here.

    The doc MUST include:
    - Exact command to run the daemon with a config file (include `--config ...` and `-v/-vv` guidance).
    - Baseline model note: the Phase 2 proof uses **`tiny.en`** as the reference model (path remains config-driven).
    - How to capture artifacts:
      - stdout transcript JSONL (save to `transcript.jsonl`)
      - stderr instrumentation logs (save to `timing.log`)
    - A clear pass/fail checklist aligned to ROADMAP Phase 2 success criteria:
      - JSONL is one JSON object per line (no log contamination).
      - The “utterance summary” JSON includes per-stage monotonic durations and a total end-to-end duration field.
      - Define acceptance: p95 end-to-end < 500ms across N short phrases on the target machine.
    - A short “how to interpret results” section mapping stage fields to the roadmap budgets (VAD <= 50ms, STT <= 200ms, total <500ms).
    - Known caveats (CPU load, mic device differences, background noise).
  </action>
  <verify>
    <automated>test -f docs/latency-baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/latency-baseline.md` exists.
    - The doc includes literal strings `transcript.jsonl` and `timing.log`.
    - The doc defines p95 < 500ms threshold and names Phase 2 metric as “end-of-speech → transcript JSONL emit”.
    - The doc references Phase 2 success criteria from `.planning/ROADMAP.md`.
  </acceptance_criteria>
  <done>
    A developer can run a single documented procedure on target hardware to collect evidence and decide pass/fail for the Phase 2 latency budget.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add env-gated concurrency stress test for pipeline concurrency</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/02-pipeline-core/02-CONTEXT.md
    - src/audio/mod.rs
    - src/input/inject.rs
  </read_first>
  <files>tests/concurrency_stress.rs</files>
  <action>
    Create `tests/concurrency_stress.rs` as an env-gated/ignored integration test that stress-exercises the Phase 2 thread topology.

    Constraints:
    - MUST NOT require real audio devices or model files by default.
    - Gate under `RUN_STRESS_TESTS=1` and `#[ignore]` so default `cargo test` stays fast.

    Assertions:
    - No deadlock/hang over a fixed time window.
    - Bounded queue send path never blocks (exercise drop-oldest behavior).
    - Observable progress on the “drain/process” side while “STT work” is concurrently running (use a stub STT worker, not whisper).
  </action>
  <verify>
    <automated>RUN_STRESS_TESTS=1 cargo test --test concurrency_stress -- --include-ignored</automated>
  </verify>
  <acceptance_criteria>
    - `tests/concurrency_stress.rs` exists and is ignored/env-gated (does not run by default).
    - With `RUN_STRESS_TESTS=1`, the test exits 0 in finite time.
  </acceptance_criteria>
  <done>
    There is a reproducible stress test artifact supporting Phase 2 success criterion #5 (concurrent audio capture + recognition pipeline work).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update validation frontmatter after Wave 0 scaffolding exists</name>
  <read_first>
    - .planning/phases/02-pipeline-core/02-VALIDATION.md
    - .planning/phases/02-pipeline-core/02-01-PLAN.md
  </read_first>
  <files>.planning/phases/02-pipeline-core/02-VALIDATION.md</files>
  <action>
    Update `.planning/phases/02-pipeline-core/02-VALIDATION.md` frontmatter after the Wave 0 test files exist:
    - Set `wave_0_complete: true` once the Wave 0 files listed in the doc exist.
    - Set `nyquist_compliant: true` once the sign-off checklist is satisfied for this phase (may remain false until the executor confirms it).

    This task is bookkeeping-only; do not change the validation strategy content beyond status fields unless required by executed work.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-pipeline-core/02-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `02-VALIDATION.md` frontmatter fields exist and can be updated without breaking YAML.
    - The task updates `wave_0_complete` to true if Wave 0 files exist; otherwise leaves it false with no other changes.
  </acceptance_criteria>
  <done>
    Validation bookkeeping no longer blocks Nyquist tracking once Wave 0 is in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---------|-------------|
| stdout JSONL → downstream consumer | Contract boundary; must remain machine-readable |
| stress harness → pipeline | Test must not rely on hardware; avoid flaky coupling |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-02-10 | R (Repudiation) | latency proof | mitigate | Document exact commands + artifact capture + pass/fail criteria |
| T-02-11 | D (DoS) | concurrency | mitigate | Add stress test to detect deadlocks/hangs and bounded-queue regressions |
</threat_model>

<verification>
- `test -f docs/latency-baseline.md` passes.
- `RUN_STRESS_TESTS=1 cargo test --test concurrency_stress -- --include-ignored` passes (opt-in).
</verification>

<success_criteria>
- A reproducible Phase 2 latency proof procedure exists and is aligned with roadmap success criteria.
- A concurrency stress test exists to support the “concurrent recognition + audio capture” requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-core/02-04-SUMMARY.md`
</output>

