---
phase: 02-pipeline-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/config.rs
  - config.example.yaml
  - tests/drop_oldest_queue.rs
  - tests/jsonl_schema.rs
  - tests/stt_smoke.rs
  - tests/wake_word.rs
autonomous: true
requirements: [ACT-02, STT-01]
user_setup:
  - service: system-packages
    why: "Build/run native ML dependencies locally (on-device only)"
    env_vars:
      - name: ORT_DYLIB_PATH
        source: "Local environment variable pointing at `libonnxruntime.so` (Silero VAD runtime). Optional if installed in system library path."
    dashboard_config: []
    notes:
      - "Install `cmake` locally to build whisper.cpp bindings via `whisper-rs` (research flagged missing `cmake` on current machine)."
      - "Ensure ONNX Runtime shared library (`libonnxruntime.so`, typically 1.22.x) is discoverable at runtime for `silero-vad-rust` (via system library path or `ORT_DYLIB_PATH`)."

must_haves:
  truths:
    - "Daemon can be configured with local wake-word and STT model paths (no network)."
    - "Test harness exists to validate STT and wake-word behavior in an opt-in, model-gated way."
  artifacts:
    - path: "Cargo.toml"
      provides: "Phase 2 pipeline dependencies (Silero VAD, whisper-rs, sherpa-onnx, bounded queue, JSON encoding)"
    - path: "src/config.rs"
      provides: "Config schema for VAD/STT/wake/pipeline; strict parsing + fail-fast validation hooks"
    - path: "tests/stt_smoke.rs"
      provides: "Env-gated STT smoke test harness scaffold"
    - path: "tests/wake_word.rs"
      provides: "Env-gated wake-word smoke test harness scaffold"
  key_links:
    - from: "src/config.rs"
      to: "runtime model loaders"
      via: "validated model_path fields"
      pattern: "model_path"
    - from: "tests/*"
      to: "CI/default test run"
      via: "env-gated + ignored heavy tests"
      pattern: "RUN_(STT|KWS)_TESTS"
---

<objective>
Establish Phase 2’s dependency + configuration + test scaffolding foundation: local-only model paths for VAD/STT/wake word, and opt-in heavy tests that can be run on developer machines without breaking default `cargo test`.

Purpose: Unblock deterministic implementation in later waves while keeping CI fast and respecting “on-device only” constraints.
Output: Updated dependencies + config schema + Wave 0 test harness files (including env-gated integration tests).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-pipeline-core/02-CONTEXT.md
@.planning/phases/02-pipeline-core/02-RESEARCH.md
@.planning/phases/02-pipeline-core/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 dependencies (per D-13, D-17, D-19)</name>
  <read_first>
    - Cargo.toml
    - .planning/phases/02-pipeline-core/02-RESEARCH.md
  </read_first>
  <files>Cargo.toml</files>
  <action>
    Add Phase 2 crates to `Cargo.toml` as normal dependencies:
    - `silero-vad-rust` (Silero VAD) per D-09/D-10
    - `whisper-rs` (whisper.cpp integration) per D-13
    - `sherpa-onnx` (wake word) per D-17
    - `crossbeam-channel` (bounded queue with try ops for drop-oldest) per D-02/D-03
    - `serde_json` (JSONL encoding) per D-19/D-22

    Constraints:
    - Do not introduce any network-dependent crates or runtime calls (project constraint: on-device only).
    - Keep STT work off Tokio (project constraint); no async STT APIs.
  </action>
  <verify>
    <automated>cargo test -q</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -q` passes with the new dependencies added (even if later modules are not yet implemented).
    - No new dependency introduces runtime network access as part of its normal operation (use local paths only).
  </acceptance_criteria>
  <done>
    Cargo compiles and tests pass with Phase 2 dependency additions only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend config schema for pipeline/VAD/STT/wake (per D-15, D-17, D-19/D-20)</name>
  <read_first>
    - src/config.rs
    - config.example.yaml
    - .planning/phases/02-pipeline-core/02-CONTEXT.md
    - .planning/phases/02-pipeline-core/02-PATTERNS.md
  </read_first>
  <files>src/config.rs, config.example.yaml</files>
  <action>
    Extend `Config` with new strict sections (each `#[serde(deny_unknown_fields)]`), matching established parsing patterns:
    - `pipeline` section:
      - verbosity for detailed stage events (per D-22; default: summary only)
      - listen window duration seconds for wake-word LISTENING (per D-18; default ~5s)
    - `vad` section:
      - thresholds (start/stop hysteresis) per D-10 (exact numeric defaults at discretion)
      - minimum speech duration (~100ms) per D-12
      - end-of-speech silence (~400ms) per D-11
      - preroll_ms (~100–200ms) per D-06
      - tail_ms (~100–200ms) per D-07
      - max_utterance_secs (~10s) per D-08
    - `stt` section:
      - `model_path` (string/path) per D-15
      - optional STT tuning knobs if required by whisper-rs defaults (keep minimal; no partials per D-14)
    - `wake` section:
      - wake word enable flag
      - model artifact paths required by sherpa-onnx keyword spotter (encoder/decoder/joiner/tokens/keywords) per D-17
      - optional trigger threshold / boost if exposed (keep minimal)

    Add load-time validation helpers (called by main later, but implemented here):
    - Validate required files exist for enabled features (stt.model_path exists, wake model files exist).
    - Return contextual `anyhow::Result` errors (no panics/unwrap).

    Update `config.example.yaml` to include the new sections with sensible defaults and comments that:
    - make explicit “stdout is JSONL transcript only; logs on stderr” per D-19/D-20
    - point to default model file locations (document-only; do not download).
  </action>
  <verify>
    <automated>cargo test -q</automated>
  </verify>
  <acceptance_criteria>
    - Config parsing remains strict (unknown fields fail) and never panics on wrong types.
    - New config sections exist in `config.example.yaml` with documented defaults.
    - Validation helpers return actionable errors when model files are missing.
  </acceptance_criteria>
  <done>
    Config schema supports all Phase 2 on-device model paths and pipeline parameters needed by downstream implementation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Wave 0 tests (env-gated heavy tests + fast unit invariants)</name>
  <read_first>
    - .planning/phases/02-pipeline-core/02-VALIDATION.md
    - .planning/phases/02-pipeline-core/02-PATTERNS.md
    - tests/config_parse.rs
  </read_first>
  <files>tests/drop_oldest_queue.rs, tests/jsonl_schema.rs, tests/stt_smoke.rs, tests/wake_word.rs</files>
  <action>
    Create the Wave 0 test files required by `02-VALIDATION.md`:
    - `tests/drop_oldest_queue.rs`: unit tests for “bounded queue with drop-oldest” semantics using the exact algorithm that the pipeline will use (e.g., `try_send` then `try_recv` one item then `try_send` again).
    - `tests/jsonl_schema.rs`: unit tests validating JSONL event encoding invariants (required fields present, timing fields non-negative, stable key names).
    - `tests/stt_smoke.rs`: integration-style, env-gated test harness (skipped unless `RUN_STT_TESTS=1`) that can load a local whisper model path from env/config and run one trivial transcription (keep `#[ignore]` plus env gate so default `cargo test` stays fast).
    - `tests/wake_word.rs`: integration-style, env-gated test harness (skipped unless `RUN_KWS_TESTS=1`) that can load local sherpa-onnx artifacts and exercise a minimal decode loop.

    Constraints:
    - Do not download any model artifacts in tests.
    - Default `cargo test` must remain green without models present.
  </action>
  <verify>
    <automated>cargo test -q</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -q` passes on a machine with no model files present.
    - When env vars are enabled and model paths exist, the heavy tests can be run via the commands listed in `02-VALIDATION.md`.
  </acceptance_criteria>
  <done>
    Wave 0 test harness exists and is compatible with both fast CI and opt-in local model testing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---------|-------------|
| config file → daemon | Untrusted local input; malformed config must not crash the daemon |
| filesystem paths → model loader | Missing/invalid paths must fail fast with actionable errors |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-02-01 | D (DoS) | `src/config.rs` parsing | mitigate | Strict `deny_unknown_fields` + contextual errors; no panics on wrong types |
| T-02-02 | D (DoS) | model path validation | mitigate | Validate file existence at startup; clear stderr error and exit before spawning threads |
| T-02-03 | R (Repudiation) | test harness gating | accept | Env-gated tests are developer-controlled; document exact commands in validation file |
</threat_model>

<verification>
Run `cargo test -q` after each task. Confirm the env-gated heavy tests are present with the expected names and can be invoked via the `02-VALIDATION.md` commands (they may still be ignored/skipped unless env is set).
</verification>

<success_criteria>
- `Cargo.toml` includes Phase 2 deps required by locked decisions (whisper-rs, sherpa-onnx, Silero VAD, bounded queue, serde_json).
- Config schema includes `vad`, `stt`, `wake`, and `pipeline` sections with strict parsing and load-time validation hooks.
- Wave 0 tests exist and default `cargo test -q` is green without local models.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-core/02-01-SUMMARY.md`
</output>

