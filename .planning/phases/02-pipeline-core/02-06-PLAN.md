---
phase: 02-pipeline-core
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - docs/latency-baseline.md
  - docs/latency-proofs/phase-02-target-hardware/README.md
  - docs/latency-proofs/phase-02-target-hardware/RESULTS.template.md
autonomous: true
gap_closure: true
requirements: [STT-04]
user_setup: []

must_haves:
  truths:
    - "The repo contains an in-repo template + instructions for archiving target hardware proof artifacts (transcript.jsonl, timing.log, computed p95)."
    - "A human can run the procedure once on target hardware and commit the evidence in a consistent location without inventing new structure."
  artifacts:
    - path: "docs/latency-proofs/phase-02-target-hardware/README.md"
      provides: "Where to store artifacts + how to name runs + what must be captured"
    - path: "docs/latency-proofs/phase-02-target-hardware/RESULTS.template.md"
      provides: "Fill-in template including computed p95 and machine metadata"
  key_links:
    - from: "docs/latency-baseline.md"
      to: "docs/latency-proofs/phase-02-target-hardware/"
      via: "proof artifacts section"
      pattern: "latency-proofs/phase-02-target-hardware"
---

<objective>
Close the “target hardware proof artifacts” gap by adding an in-repo place to store evidence and a template that captures the required outputs (stdout JSONL, stderr timing logs, and computed p95) so Phase 2 can be marked complete after a single human run.

Purpose: Verification requires committed evidence, not just a runbook.
Output: Proof artifact templates + doc wiring to the baseline procedure.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-pipeline-core/02-VERIFICATION.md
@docs/latency-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add in-repo proof artifact template directory for Phase 2 target hardware runs</name>
  <read_first>
    - .planning/phases/02-pipeline-core/02-VERIFICATION.md
    - docs/latency-baseline.md
  </read_first>
  <files>
    docs/latency-proofs/phase-02-target-hardware/README.md
    docs/latency-proofs/phase-02-target-hardware/RESULTS.template.md
  </files>
  <action>
    Create a proof-archive directory under `docs/latency-proofs/phase-02-target-hardware/` containing:

    1) `README.md` with:
       - The purpose: store Phase 2 target-hardware evidence for the “p95 < 500ms” claim.
       - Required artifacts per verification gap:
         - `transcript.jsonl` (stdout capture)
         - `timing.log` (stderr capture)
         - computed p95 for `e2e_ms`
       - A recommended run naming convention (e.g., `run-YYYYMMDD-HHMM-{hostname-or-machine}/`) and where to place the 3 artifacts.
       - A short note that stdout must remain JSONL-only and logs must go to stderr (D-20), so captures should use `> transcript.jsonl 2> timing.log`.

    2) `RESULTS.template.md` capturing:
       - Machine metadata (CPU model, RAM, OS/kernel, power governor if known)
       - Daemon version/git commit, build profile (`--release`), config file path used
       - A section to paste the computed p50/p95/p99 values for `e2e_ms`
       - A section for notes (environment noise, mic device, anomalies)

    Keep this template text-only and repo-friendly (no binary attachments).
  </action>
  <verify>
    <automated>
      test -f docs/latency-proofs/phase-02-target-hardware/README.md &&
      test -f docs/latency-proofs/phase-02-target-hardware/RESULTS.template.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Both template files exist in-repo.
    - `README.md` explicitly lists `transcript.jsonl`, `timing.log`, and “computed p95”.
    - `RESULTS.template.md` has clearly labeled sections for machine metadata and p95 results.
  </acceptance_criteria>
  <done>
    The repository contains a ready-to-fill template and a canonical place to archive Phase 2 target-hardware proof artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire baseline procedure doc to the proof artifact template location</name>
  <read_first>
    - docs/latency-baseline.md
    - docs/latency-proofs/phase-02-target-hardware/README.md
  </read_first>
  <files>docs/latency-baseline.md</files>
  <action>
    Update `docs/latency-baseline.md` to point readers at the canonical proof-archive directory:
    - Add a short section like “Archiving evidence in-repo” linking to `docs/latency-proofs/phase-02-target-hardware/`.
    - Ensure the baseline doc clearly states which captured files to commit and where.

    Do not change the stdout capture procedure itself beyond referencing the archive location.
  </action>
  <verify>
    <automated>rg -n "phase-02-target-hardware" docs/latency-baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/latency-baseline.md` references `docs/latency-proofs/phase-02-target-hardware/` by path.
  </acceptance_criteria>
  <done>
    Verification gap #3 is closed: after a human run, there is a clear repo location + template for committing evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---------|-------------|
| human-run artifacts → repo evidence | Evidence must be attributable and consistently structured |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-02-30 | R (Repudiation) | target hardware proof | mitigate | Template captures git commit + machine metadata + p95 results + raw captures referenced by filename |
</threat_model>

<verification>
- Template files exist at the documented paths.
- `docs/latency-baseline.md` links to the proof archive directory.
</verification>

<success_criteria>
- A single target-hardware run can be archived in-repo using the provided template, satisfying the verifier’s evidence requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-core/02-06-SUMMARY.md`
</output>

