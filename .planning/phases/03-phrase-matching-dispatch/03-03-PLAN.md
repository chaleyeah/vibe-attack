---
wave: 3
depends_on: [2]
files_modified:
  - src/pipeline/coordinator.rs
  - src/main.rs
autonomous: true
---

# Wave 3: Pipeline Integration

This wave wires the `Dispatcher` into the main audio pipeline.

## Tasks

<task>
<read_first>
- src/pipeline/coordinator.rs
</read_first>
<action>
1. Update `spawn_pipeline` to accept `macro_tx: crossbeam_channel::Sender<crate::input::inject::MacroCmd>`.
2. Spawn the `Dispatcher` thread.
3. Insert the `Dispatcher` between `SttService` and the `JsonlWriter`.
   - `SttService` -> `Dispatcher` -> `JsonlWriter`.
</action>
<acceptance_criteria>
- Pipeline thread topology matches D-10 and D-11.
- STT results still reach the JSONL output thread.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/main.rs
</read_first>
<action>
1. Pass the existing `macro_tx` into `spawn_pipeline`.
</action>
<acceptance_criteria>
- `cargo build` passes.
- App starts and logs "pipeline_started".
</acceptance_criteria>
</task>

## Verification

### Automated Tests
- None.

### Manual Verification
- Run daemon with a dummy macro config.
- Verify that recognized phrases trigger "Firing macro" logs.

## must_haves
- [ ] Dispatcher thread spawned correctly.
- [ ] Macros are injected into the input thread.
