---
wave: 2
depends_on: [1]
files_modified:
  - Cargo.toml
  - src/pipeline/sound.rs
  - src/pipeline/dispatcher.rs
autonomous: true
---

# Wave 2: Sound Feedback & State Management

This wave implements the audio playback backend and the state flag registry.

## Tasks

<task>
<read_first>
- Cargo.toml
</read_first>
<action>
1. Add `rodio = "0.17"` to `Cargo.toml`.
</action>
<acceptance_criteria>
- `cargo check` passes.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/pipeline/mod.rs
</read_first>
<action>
Create `src/pipeline/sound.rs`:
1. Implement a `SoundPlayer` that uses `rodio`.
2. Support playing audio files by path.
3. Sound should be fire-and-forget (non-blocking).
</action>
<acceptance_criteria>
- Sound player initializes without error if a default output device exists.
- `SoundPlayer::play(path)` does not block the calling thread.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/pipeline/matcher.rs
</read_first>
<action>
Create `src/pipeline/dispatcher.rs`:
1. Implement `DispatcherState` holding a `HashMap<String, bool>` in an `Arc<RwLock>`.
2. Implement `Dispatcher` logic:
   - Match transcript against config.
   - If match found:
     - Check `if_flag`.
     - Play `sound` if configured.
     - Send `MacroCmd` to injection channel.
     - Set `set_flag` if configured.
</action>
<acceptance_criteria>
- Dispatcher correctly identifies matches.
- Dispatcher correctly skips macros if `if_flag` condition is not met.
</acceptance_criteria>
</task>

## Verification

### Automated Tests
- Unit tests for `Dispatcher` using mock channels.

### Manual Verification
- None (requires wiring).

## must_haves
- [ ] `rodio` dependency added.
- [ ] Sound playback is non-blocking.
- [ ] Boolean flags persist for the duration of the process.
