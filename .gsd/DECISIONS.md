- Use unix milliseconds for wall clock timestamps (allowed by D-21) to avoid new time-format deps.
- Keep segmentation logic deterministic/testable by accepting scored frames, while providing a Silero-scoring path built on a 512-sample sliding window.
- Wake-word requires continuous audio buffering; CPAL callback always pushes to ringbuf, and PTT/LISTENING gating happens on the pipeline thread.
- Silero VAD is initialized with CPU-only options (`force_onnx_cpu: true`) to match Phase 2 baseline constraints.
- Stdout is reserved for JSONL only via a dedicated output thread; all status/instrumentation stays on stderr via tracing.
- Phase 2 latency proof metric is end-of-speech → transcript JSONL emit; end-of-speech → first key event is validated in Phase 3 dispatch.
- `e2e_ms` is defined as `output_done_ms - vad_done_ms` and computed at JSONL emission time using monotonic markers
- `vad_ms` measures compute cost (Instant elapsed) for Silero scoring + segmentation per frame, accumulated only while an utterance is active

---

## Decisions Table

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M007 / S01 — discovered during research as the only real bug surfaced by the cleanup pass. | architecture | Pin profiles directory canonical format to {name}/pack.yaml subdirectories (not flat *.yaml files) | load_profiles in src/ui/config_app.rs is fixed in M007/S01 to read only subdirectory profiles. Pack::load_from_dir and handle_switch_profile already use this format; the UI is now aligned. Flat *.yaml profiles are no longer recognized. | Two coexisting profile formats on disk meant the config UI could list a profile that the switch command could not load — a latent UX bug. The subdirectory format is what Pack::load_from_dir and handle_switch_profile already use, and it matches the export format used by the pack import/export tests, so converging on it requires the smallest behavior change. The alternative (supporting both formats) would double the maintenance surface and embed the inconsistency permanently. | If users with legacy flat .yaml profiles complain, add a one-time migration step that wraps each flat profile into its own subdirectory. Do not re-introduce flat-format support in the loader. | agent |
| D002 | M007 — established as part of the documentation cleanup milestone; intended to persist as a project-wide convention going forward. | convention | Every public item in src/ requires a /// doc comment | M007 establishes a project convention: every pub fn/struct/enum/trait/type/const/mod in src/ must carry a /// doc comment. Enforced by the Python audit script in M007-RESEARCH.md, which is run at the end of S03, S04, and milestone close. | The codebase will continue to grow. Without an enforced documentation bar, the comprehension cost for new contributors compounds with every undocumented addition. A simple "must have ///" rule is mechanically auditable and sets the floor; reviewers can still push for substance during PR review. Coverage percentages or doctest requirements are heavier and not justified at the current project scale. | Revisit if doc comments become superficial filler; in that case, add a quality review step or shift to required examples for non-trivial items. | agent |
| D003 | M007 / S02 — applied to all existing instances in src/; intended to persist as a project convention. | convention | Every unsafe block and #[allow(...)] in src/ requires an adjacent justifying comment | M007/S02 enforces: every `unsafe impl`, `unsafe fn`, and `#[allow(...)]` annotation in src/ must be preceded by a comment explaining the safety invariant or the lint justification. The unsafe impl Send/Sync on Dispatcher is the canonical example — it is correct (rodio OutputStream is non-Send but only ever invoked from the dispatcher's owning thread) and now carries a // SAFETY: comment. | unsafe and lint allows are the two places where the compiler stops protecting future readers. Without an explanation, a later contributor cannot tell whether the annotation is load-bearing or removable. Forcing a one-line justification at the annotation site makes the invariant (or the lint-justification) discoverable without a git blame archaeology session. | Solid convention; no obvious reason to revisit. | agent |
| D004 | M007 — set during planning as the primary scope guardrail. | scope | M007 explicitly excludes behavior changes; the only functional change is the load_profiles bug fix | M007 ships zero behavior changes other than the load_profiles UI bug fix. No new features, no error-handling refactors, no API changes, no packaging changes, no CI changes. Visibility narrowing (DispatcherState pub→pub(crate)) is permitted because it is verifiably internal-only. | Cleanup milestones that quietly include behavior tweaks are how regressions enter codebases. Pinning M007 to "docs + dead code + one targeted bug fix" makes the diff easier to review and gives a clean rollback boundary if anything regresses. New features and refactors get their own milestone where the design rationale gets proper attention. | If a clearly-trivial behavior cleanup is discovered mid-slice, lift it into a new slice with explicit success criteria rather than smuggling it into a doc commit. | agent |
